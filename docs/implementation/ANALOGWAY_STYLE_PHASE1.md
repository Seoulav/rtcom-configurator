# Analog Way 방식 화면 개편 1·2단계 (0.8.0)

- 요청: "https://www.analogway.com/configurator 와 거의 비슷하게" + 사용자 제공 XDM 사진 압축 파일
- 기준 커밋: `0228125` (0.7.0 병합)
- 참고 범위: Analog Way 구성기의 화면 흐름과 조작 방식(섀시 카드 선택, 후면 슬롯 클릭, 팝업 선택, 요약)만 참고했습니다. Analog Way의 이미지·로고·문구·코드는 사용하지 않았습니다. Analog Way 사이트는 작업 환경 브라우저에서 인증서 문제로 열리지 않아, 페이지 원문(단계 구성, 카드 선택 방식)으로 흐름을 확인했습니다.

## 사용자 제공 자료

| 자료 | 수량 | 처리 |
|---|---|---|
| 입력 카드 후면 판넬 (HI, HIS, DPI, CIS, FIS, SIS) | 6 | 판넬 금속면만 잘라 1200px WebP |
| 출력 카드 후면 판넬 (HOS, DPOS, COS, FOS, SOS, WOS) | 6 | 〃 |
| 프레임 전면 (XDM-12, XDM-36) | 2 | XDM-12만 사용(배경 투명 영역 제거 후 WebP). XDM-36은 사선 사진이라 매뉴얼의 정면 사진으로 대체 |
| 국문 매뉴얼 PDF (KV08, 2025-09-02, 55쪽) | 1 | 전면 5장(XDM-20·36·72·144·216), 후면 5장(XDM-12·20·36·72·144) 추출 |

- 원본은 약 30MB, 결과는 약 420KB입니다. 원본은 `.source-materials/`(Git 제외)에 두고 저장소에는 넣지 않았습니다.
- 가공 스크립트 `scripts/tools/prepare_xdm_images.py`로 같은 결과를 다시 만들 수 있습니다. 판넬 위로 튀어나온 기판 부품과 흰 배경은 "가로폭의 85% 이상을 채우는 줄"만 판넬로 인정하는 방식으로 제외했고, 판넬 안의 얇은 밝은 줄에서 끊기지 않도록 폭의 2% 이하 틈은 이었습니다. 12종 모두 약 1200×125(가로:세로 ≈ 9.7:1)로 맞췄고 눈으로 확인했습니다.
- 기존 카탈로그 추출 카드 JPG 12장은 판넬 사진으로 교체했습니다.
- 사용자는 "업체 프레임 뒷면 사진을 받기 전까지 PDF 브로셔의 뒷면을 활용"을 요청했습니다. 48쪽 종합 카탈로그(브로셔)를 전부 확인했지만 XDM 프레임 후면 사진은 없었습니다(4쪽 제품군 단체 사진, 6~9쪽 카드, 10~12쪽 전송기만 수록). 이어서 받은 국문 매뉴얼 PDF의 6~12쪽에 강조색이 없는 원본 후면 사진이 있어 이를 사용했습니다. 스크립트 `scripts/tools/extract_manual_frames.py`는 사진을 (쪽 번호, 가로, 세로)로 찾아 WebP로 저장합니다.
- 이전에 쓰던 매뉴얼 후면 JPG 5장은 입력·출력 영역에 파란색·빨간색이 칠해진 저해상도 캡처라 삭제했습니다.

## 후면 사진 위 슬롯 표시

- `src/app.js`의 `rearPhotos`에 모델별 사진 경로, 쪽 번호, 사진 크기, 입력·출력 카드 영역(사진 픽셀 좌표)을 둡니다. 화면은 이 좌표를 백분율로 바꿔 사진 위에 슬롯 격자를 겹칩니다.
- 좌표는 카드 고정 나사의 위치를 3배 확대 이미지에서 읽어 나사 간격으로 계산했습니다. 처음 측정에서 XDM-36을 2배 확대 좌표로 잘못 읽어 왼쪽에 사진 속 카드 한 줄이 비쳤고, 나사 기준으로 다시 재서 고쳤습니다. XDM-72·144는 1~2px 보정, XDM-12·20은 변경 없음.
- 매뉴얼 후면 사진에는 카드가 꽂혀 있으므로, 빈 슬롯은 불투명한 블랭크 커버로 덮어 사용자가 고른 카드만 보이게 합니다.
- `tests/site.test.cjs`는 좌표가 사진 안에 있고 슬롯 비율이 판넬(9.7:1)과 맞는지 검사합니다. 좌표가 통째로 밀린 오류는 비율 검사로 잡히지 않으므로 캡처로 확인했습니다.
- XDM-216은 매뉴얼에 후면 사진이 없어 그림 랙을 유지합니다.

## 화면 변경

| 단계 | 이전 | 이후 |
|---|---|---|
| 02 섀시 | 목록 + 미리보기 | 전면 사진 카드 그리드(XDM 6종 실제 전면 사진). 입력·출력 슬롯 수, 최대 채널. 전면 사진이 없는 XDM-288은 제품군 사진, 슬롯 자료가 없는 모델(XDM-288, SPX, VDM)은 "슬롯 구성 제조사 확인 필요" |
| 03 카드 | 논리 슬롯 도식 + 오른쪽 카드 목록 + 매뉴얼 사진 상시 표시 | XDM-12~144는 매뉴얼 실제 후면 사진 위 슬롯, XDM-216은 후면 랙 그림. 빈 슬롯을 누르면 카드 선택 팝업(`<dialog>`). 장착 슬롯에 실제 판넬 사진. 오른쪽 구성 요약 |

- 랙 배치는 매뉴얼 후면 사진 기준입니다: XDM-12는 가로 카드(왼쪽 입력·오른쪽 출력), XDM-20·36은 세로 카드(왼쪽 입력·오른쪽 출력), XDM-72·144·216은 세로 카드(위 입력·아래 출력, 한 줄 18슬롯).
- 세로 슬롯의 판넬 사진은 CSS container query 단위(`cqh`, `cqw`)로 슬롯 크기에 맞춰 -90° 회전합니다.
- XDM-144·216은 랙이 매우 길어지므로 랙 폭을 `2040px / 줄 수`로 제한하되 최소 560px(슬롯 약 26px)를 보장하고, 좁은 화면에서는 랙 영역만 좌우로 밉니다.
- 슬롯 이름은 "IN 1", "OUT 1"로 줄이고 전체 이름은 `aria-label`과 `title`에 둡니다.

## 접근성

- 팝업은 `<dialog>`의 `showModal()`을 사용해 뒤 화면을 비활성화합니다. 열리면 현재 장착 카드(없으면 첫 카드)에 포커스를 둡니다.
- Esc, 닫기 버튼, 바깥 영역 클릭, 카드 선택, 슬롯 비우기 중 어느 것으로 닫아도 누른 슬롯 버튼으로 포커스가 돌아옵니다.
- 팝업이 열린 상태는 저장하지 않는 화면 전용 값(`modalSlot`)이라 저장 파일과 실행 취소 기록에 영향이 없습니다.

## 유지한 계약

LocalStorage key `rtcom.configuration.v1`, JSON schema 3, `catalogVersion`, 슬롯 ID(`in-1`…`out-54`, `in-a`…), 구성기 3/21/26, 계산 로직(`core.js`)은 변경하지 않았습니다. 옛 `chassisView`, `cardsViewV3`는 새 함수로 대체되어 삭제했습니다.

## 되돌리는 방법

이 PR의 병합 커밋을 revert하면 0.7.0 화면으로 돌아갑니다. 저장 형식이 같아 어느 쪽에서 저장한 구성도 불러올 수 있습니다.

## 남은 일 (3단계와 추가 자료)

- 업체의 빈 프레임 후면 사진(고해상도)을 받으면 `rearPhotos`의 사진과 좌표 교체, XDM-216 후면 사진 추가
- XDM-288 전면·후면 사진과 슬롯 자료
- 3단계: 후면 구성 이미지와 부품번호 목록이 들어간 구성 요약서(PDF 인쇄) 개편
- 옛 화면 전용 CSS 선택자 정리(현재 쓰이지 않지만 남아 있음)
