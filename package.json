{
  "name": "rtcom-configurator",
  "version": "0.1.0",
  "private": true,
  "description": "RTCOM internal review configurator",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "test": "node --test tests/*.test.cjs"
  }
}
