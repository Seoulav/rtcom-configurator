# 변경 기록

이 문서는 병합된 버전과 주요 작업을 시간순으로 기록합니다. 작업 중인 변경은 `Unreleased`에 적고, 원본 소스 `main`에 병합할 때 버전 번호를 확정합니다.

## Unreleased

## 0.17.0

- 장착한 카드의 포트가 모두 보이게 했습니다. 슬롯 번호표(IN 1, OUT 1 …)가 판넬 첫 포트 위에 얹혀, SPX-HIS8은 HDMI 8개 중 7개, SPX-COS12는 RJ45 12개 중 10개만 보였습니다(사용자 제보, SPX-M3236 스크린샷). 카드를 장착한 슬롯에서는 번호표를 숨기고, 마우스를 올리거나 키보드로 선택하면 보입니다. 빈 슬롯의 번호표는 그대로입니다. XDM·SPX·VDM 공통입니다.

## 0.16.0

- 카드 판넬 좌우 끝 잘림 수정: 사진·도면 위 슬롯의 1px 테두리가 판넬을 덮고 판넬을 그만큼 줄였습니다. 휴대폰의 좁은 세로 슬롯(XDM-20 약 23px)에서는 폭의 약 9%가 가려져 좌우 끝이 잘려 보였습니다. 사진 슬롯은 테두리 없이 판넬이 꽉 차게 하고, 마우스를 올리면 안쪽 윤곽선으로 강조합니다(사용자 제보, XDM-20 FOS100 스크린샷).

## 0.15.0

- VDM 섀시 선택 화면: 실물 전면 사진이 없는 7종(8X·32X·64X·80X·128X·180X·256X)에 매뉴얼 전면 선 도면(VDM 국문 매뉴얼 KV07 PDF pp.12–20)을 넣었습니다. 16X·48X는 기존 실물 사진을 그대로 씁니다. 이미지 설명(alt)은 "전면 사진 / 전면 도면"으로 구분합니다.
- 알티컴 홈페이지(rtcomav.com)를 확인했지만 VDM 모델별 프레임 사진은 없었습니다(라인업 단체 사진 1장과 이미 쓰는 매뉴얼 PDF뿐). 조사 결과는 `docs/implementation/VDM_FRONT_DRAWINGS.md`에 있습니다.

## 0.14.0

- 휴대폰에서 카드가 잘리던 문제 수정: 터치 기기용 "버튼 최소 높이 44px" 규칙이 사진·도면 슬롯에도 적용되어, 슬롯 높이가 44px보다 작은 프레임(SPX-M3236 등)에서 슬롯이 서로 겹치고 판넬이 잘렸습니다. 슬롯은 이 규칙에서 제외했습니다. SPX 가로 카드 사진은 휴대폰 화면 폭에 맞춰 줄어듭니다.
- 뒤로가기: 단계를 옮길 때마다 브라우저 방문 기록을 남겨(주소는 그대로) 휴대폰 뒤로가기·앞으로가기가 이전·다음 단계로 움직입니다. 이전에는 뒤로가기를 누르면 사이트 밖(예전 주소)으로 나가 404가 자주 났습니다.
- 사이트 안의 없는 주소로 들어오면 `404.html`이 구성기 첫 화면으로 보냅니다.

## 0.13.0

- VDM 8종(8X·32X·48X·64X·80X·128X·180X·256X)의 카드 슬롯 화면을 그림 랙에서 매뉴얼 후면 도면으로 바꿨습니다(VDM 국문 매뉴얼 KV07 PDF pp.12–20). 도면 위에 빈 슬롯 커버와 장착 카드 판넬을 보드 경계선에 맞춰 겹쳐 표시합니다. 16X는 기존대로 실물 후면 사진을 씁니다(p.7).
- 도면 기준으로 한 줄 보드 수를 바로잡았습니다: 80X·128X는 11열(남는 칸은 제어 보드·빈칸), 256X는 128X와 같은 랙 2대(왼쪽 랙 입력·출력 1–32, 오른쪽 랙 33–64). 슬롯 수와 슬롯 ID는 바뀌지 않습니다.
- 한 방향 슬롯을 여러 사진 영역으로 나눠 그릴 수 있게 했습니다(`rearPhotos`의 input·output에 영역 배열 허용). 사진 설명은 실물 사진이면 "후면 사진", 도면이면 "후면 도면"으로 표시합니다.

## 0.12.0

- 후면 사진 슬롯 위치 보정: 슬롯 영역 높이가 사진 아래 설명 한 줄까지 포함해 계산되어, 아래쪽 슬롯일수록 사진보다 조금씩 내려가 있었습니다. 영역 높이를 사진 비율로 고정해 XDM·SPX·VDM 사진 슬롯이 사진 속 나사와 정확히 겹칩니다(사용자 제보 "프레임이 정교하지 않아").
- SPX 빈 슬롯 커버를 합성 이미지에서 매뉴얼 SPX-M3236 후면 사진 속 실제 블랭크 판넬(나사 줄 한 칸)로 바꿨습니다.
- VDM-256X를 추가했습니다(사용자 결정 2026-09-26). VDM 국문 매뉴얼 KV07 PDF p.20(본문 19쪽) 기준 입력·출력 보드 각 64장(256×256), 2랙 폭 39RU 독립형입니다. 섀시는 21종에서 22종이 됩니다.
- 후면은 매뉴얼 도면을 단순화해 입력(위)·출력(아래) 각 64슬롯을 32열 2줄로 표시합니다(랙 2개 × 16열).
- 슬롯 ID를 `in-64`·`out-64`까지 늘렸습니다. 기존 슬롯 ID는 그대로입니다. 모델을 추가만 했으므로 `catalogVersion`은 올리지 않았습니다. 올리면 이미 저장한 JSON을 불러올 수 없기 때문입니다.

## 0.11.0

- SPX 국문 사용자 매뉴얼(250805)을 근거로 SPX 5종을 XDM과 같은 방식으로 바꿨습니다. 섀시 선택에 전면 사진을 넣고, 후면 사진 위에 빈 슬롯(블랭크 커버)과 장착 카드 판넬을 겹쳐 표시합니다.
- SPX-M2472·M24120은 매뉴얼 후면 사진대로 세로 카드(왼쪽 입력 3 · 오른쪽 출력 6/10)로 표시합니다. M810·M1620·M3236은 가로 카드(입력 위 · 출력 아래)입니다.
- SPX 카드 판넬 4종(HIS8·HOS10·HOS12·COS12)과 블랭크 커버를 카탈로그 스캔 임시 이미지에서 매뉴얼 고해상도 이미지(폭 약 1,200px)로 교체했습니다.
- SPX-M810·M1620에 HOS12·COS12를 장착하면 11·12번 포트가 출력 10번의 분배(같은 영상)로 동작한다고 경고합니다(`SPX_PORT_SPLIT`, 매뉴얼 p.11).
- SPX 슬롯 구성 근거를 매뉴얼 pp.7–9로 바꿨습니다. 0.10에서 계산값이었던 M1620 2·2는 매뉴얼 문장("입력 카드 최대 2개 / 출력 카드 최대 2개")으로 확인됐습니다. 슬롯 수와 슬롯 ID는 바뀌지 않았습니다.

## 0.10.0

- HDMI 카드 연장: XDM-HI100·HIS100(입력), XDM-HOS100·WOS100(출력)에 "CTR100 PSE + CTR100" 한 쌍을 연결할 수 있습니다(선택 사항, 자동 연결 없음). 전원은 PSE 쪽에만 연결하고 CTR100은 전원이 필요 없으며, 두 제품 모두 DIP 스위치로 TX/RX를 설정합니다(사용자 확인 2026-09-26).
- BOM: 한 쌍마다 "XDM-CTR100 PSE" 1대와 "XDM-CTR100 (PSE 급전, 전원 불필요)" 1대를 "HDMI 연장 (PSE 쌍)" 분류로 넣습니다. CTR100 전원 공급 장비와 전원 경고는 HDBaseT 카드(CIS100·COS100)에 직접 연결한 CTR100만 셉니다.
- SPX 실제 슬롯 구성: M810 입력 1·출력 1, M1620 2·2, M3236 4·3, M2472 3·6, M24120 3·10 (SPX 매뉴얼 p.4, 카탈로그 I/O 크기, 사용자 제공 M810·M3236 후면 사진). 가로 카드를 입력(위)·출력(아래)으로 쌓는 랙에 블랭크 커버와 카드 판넬을 표시합니다. 카드 판넬·블랭크는 카탈로그 스캔에서 자른 임시 이미지입니다.
- SPX-COS12를 장착하면 SPX-RX를 12채널로 자동 연결하고, SPX-RX는 메인프레임이 CAT으로 전원을 공급(POC)한다고 안내합니다.
- 예전 SPX 저장본의 논리 슬롯(입력 A·B, 출력 A·B)은 실제 슬롯 1·2로 자동 변환합니다. 슬롯이 하나뿐인 SPX-M810의 두 번째 논리 슬롯 카드는 옮길 곳이 없어 제외합니다.
- VDM 실제 슬롯 구성: 8X 2·2, 16X 4·4, 32X 8·8, 48X 12·12, 64X 16·16, 80X 20·20, 128X 32·32, 180X 45·45 (VDM 국문 매뉴얼 KV07 PDF pp.12–19). VDM-288X는 특수 상황실용 커스텀 모델이라 논리 슬롯과 "제작 사양서 확인" 안내를 유지합니다.
- VDM-16X는 매뉴얼 후면 사진 위에 슬롯을 겹쳐 표시하고, 나머지는 매뉴얼 도면을 단순화한 그림 랙으로 표시합니다. VDM 카드 10종 판넬·블랭크 커버(매뉴얼 보드 사진), VDM-16X·48X 전면 사진을 적용했습니다.
- 슬롯 모양을 제품군별 판넬 비율(XDM 9.7, SPX 13.8, VDM 5.7)로 맞췄습니다.
- 전송기 화면에 "HDMI 카드 연장" 영역을 추가하고, CTR100·CTR100 PSE 안내에 DIP 스위치 TX/RX 설정을 적었습니다.
- 카드 선택 창의 판넬 사진에서 검은 배경 테두리를 없애고 사진만 보이게 했습니다(XDM·SPX·VDM 공통, 사용자 요청 2026-09-26).

## 0.9.0

- 전송기 연동: 카드를 장착하면 카탈로그 호환 표기에 따라 전송기를 4채널분 자동 연결합니다. XDM-CIS100 → XDM-CTR100(TX), XDM-COS100 → XDM-CTR100(RX), XDM-FIS100 → XDM-FT101, XDM-FOS100 → XDM-FR101 (종합 카탈로그 p.10·12). BOM과 CTR100 전원 경고에 바로 반영됩니다.
- 전송기 단계 개편: 카드마다 전송기를 사진 타일(모델, 역할, 핵심 사양)로 고르고 채널 수를 조정합니다. 벽부형 XDM-CT103·CR103(p.11)도 대안으로 선택할 수 있습니다.
- HDBaseT·광 카드가 없을 때도 빈 안내 대신 XDM 연동 전송기 라인업 6종(CTR100, CTR100 PSE, CT103, CR103, FT101, FR101)을 사진·연동 카드·카탈로그 쪽과 함께 보여 주고, 카드 슬롯으로 돌아가는 버튼을 둡니다.
- CTR100 전원 안내를 사용자 확인 내용으로 고쳤습니다. 매트릭스 카드(CIS100·COS100)에 연결하는 CTR100은 전원을 직접 연결해야 하며 PSE를 쓸 수 없습니다. CTR100 PSE는 CTR100·CT/CR103과 1:1 연장 구성일 때 PSE 쪽에만 전원을 주면 상대 장비는 전원이 필요 없다는 안내로 표시합니다. BOM 항목 이름은 "XDM-CTR100 전원 공급 장비"로 바꿨습니다(수량 규칙은 그대로).
- 종합 카탈로그 PDF에서 전송기 사진 6장을 추출했습니다(`scripts/tools/extract_catalog_extenders.py`).
- 이미 저장한 구성과 불러온 JSON 파일에는 자동 연결을 적용하지 않습니다. 사용자가 카드를 새로 장착할 때만 적용합니다.

## 0.8.0

- Analog Way 구성기 방식으로 섀시·카드 화면을 개편했습니다.
  - 섀시 선택: 전면 사진 카드, 입력·출력 슬롯 수와 최대 채널 표시. 슬롯 자료가 없는 프레임은 "제조사 확인 필요"로 표시합니다.
  - 카드 슬롯: 후면 랙 그림에서 빈 슬롯을 누르면 카드 선택 팝업이 열리고, 장착한 슬롯에는 실제 카드 후면 판넬 사진이 들어갑니다. XDM-20 이상은 판넬을 세로로 회전해 실제 장착 방향을 반영합니다.
  - 오른쪽 구성 요약: 입력·출력 채널 사용량, 카드별 수량, 장착 카드 수.
- 사용자 제공 XDM 카드 판넬 사진 12종과 XDM-12 전면 사진을 적용했습니다. 원본 약 30MB를 WebP로 가공했고, 카탈로그 추출 카드 JPG 12장은 교체했습니다.
- 국문 매뉴얼(KV08) PDF에서 전면 사진(XDM-20·36·72·144·216)과 강조색이 없는 후면 사진(XDM-12·20·36·72·144)을 추출했습니다. 카드 단계는 실제 후면 사진 위에 사진 속 카드 위치와 맞춘 슬롯을 겹쳐 표시합니다(좌표는 카드 고정 나사 간격으로 측정). 후면 사진이 없는 XDM-216은 그림으로 표시합니다. 업체의 빈 프레임 후면 사진을 받으면 사진과 좌표만 교체합니다.
- 입력·출력 강조색이 칠해진 옛 매뉴얼 후면 JPG 5장과 "매뉴얼 사진과 비교하기"는 실제 후면 사진 방식으로 대체되어 삭제했습니다.
- 빈 슬롯을 실물 모양의 블랭크 커버(양 끝 나사가 달린 빈 판넬)로 채웠습니다. 카드를 장착하거나 비우면 판넬이 밝게 떠오르며 바뀌는 전환 효과가 나타나고, 빈 슬롯에 마우스를 올리면 강조와 "+"가 표시됩니다. 블랭크 커버 사진이 없어 WOS100 판넬의 나사 끝부분과 금속면으로 합성했습니다. 움직임 줄이기 설정에서는 효과를 끕니다.
- 모바일에서 슬롯 이름이 잘리던 문제를 짧은 번호(IN 1, OUT 1)로 해결했습니다.
- 브라우저 검사를 17개 항목으로 늘렸습니다(팝업 열기·Esc 닫기·포커스 복귀·판넬 표시·구성 요약).

## 0.7.0

- 제품 라이브러리(31개 제품, 검색·필터·상세 dialog)와 포털 홈을 제거했습니다. 제품 정보는 AV portal에서 제공합니다.
- 상단에 AV Portal(https://seoulav.github.io/AV-Portal/) 링크를 추가했습니다. AV Portal에 RTCOM 제품이 아직 없어 제품별 링크 대신 첫 화면으로 연결합니다.
- 카탈로그 PDF(3.3MB)를 배포본에서 제외했습니다. 배포 크기는 약 4.3MB에서 약 0.8MB로 줄었습니다.
- 매트릭스 구성기를 첫 화면(`/`)으로 옮겼습니다. 0.6의 `/products`, `/tools/matrix-configurator` 주소는 구성기로 자동 이동합니다.
- 앱 안에서 구성기로 이동하면 후면 사진·카드 이미지가 깨지던 결함이 사라졌습니다(주소 변경으로 상대경로가 어긋나던 원인 제거).
- 구성기가 쓰지 않는 제품 라이브러리 스타일을 정리했습니다. 구성기 화면의 계산된 스타일은 변경 전과 같습니다.
- 사이트 구성 테스트와 브라우저 검사 스크립트(`scripts/e2e-smoke.cjs`)를 추가했습니다.
- AI 세션 운영 규칙과 저장소 역할을 문서화했습니다.
- 원본 소스 저장소와 공개 배포 저장소를 구분했습니다.

## 0.6.0

- 포털 화면과 `/`, `/products`, `/tools/matrix-configurator` 경로를 추가했습니다.
- 제품 라이브러리와 매트릭스 구성기를 포털에서 연결했습니다.
- GitHub Pages `/rtcom-av-design/` base path와 직접 접근을 검증했습니다.

## 0.5.0

- 31개 제품·시리즈 라이브러리와 제조사 카탈로그 PDF 연결을 정리했습니다.
- XDM·SPX·VDM 매트릭스 구성기, 후면 이미지, 카드 슬롯 배치를 유지했습니다.
- LocalStorage 자동 저장, JSON 백업·복원, CSV·PDF 보고서 흐름을 유지했습니다.

## 버전 규칙

- 기능 묶음이 원본 `main`에 병합될 때 마이너 버전을 올립니다. 다음 목표는 `0.11.0`입니다.
- 오류 수정과 문서 보완은 패치 버전을 올립니다.
- 버전 표시, README, CHANGELOG의 버전은 같은 병합 커밋에서 함께 갱신합니다.
