
  (()=>{
    const root=document.getElementById('rtcom-design');
    const main=root.querySelector('.rt-main');
    const nav=root.querySelector('.rt-nav');
    const assets=Object.fromEntries([...root.querySelector('template').content.querySelectorAll('img')].map(x=>[x.dataset.family,x.src]));
    const families={
      XDM:{name:'eXtreme Digital Matrix',copy:'다양한 고해상도 신호를 하나로.',tags:['HDMI · DP · SDI','CAT · 광 전송'],models:['XDM-12','XDM-20','XDM-36','XDM-72','XDM-144','XDM-216','XDM-288'],modelNotes:['12 입력 / 12 출력','20 입력 / 20 출력','36 입력 / 36 출력','72 입력 / 72 출력','144 입력 / 144 출력','216 입력 / 216 출력','상세 사양 확인 예정'],input:[['XDM-HI100','HDMI · Non-Scaler',4,'HDMI'],['XDM-HIS100','HDMI',4,'HDMI'],['XDM-DPI100','DisplayPort',4,'DP'],['XDM-CIS100','HDBaseT 3.0',4,'CAT'],['XDM-FIS100','광 전송',4,'FIBER'],['XDM-SIS100','12G-SDI',4,'SDI']],output:[['XDM-HOS100','HDMI',4,'HDMI'],['XDM-DPOS100','DisplayPort',4,'DP'],['XDM-COS100','HDBaseT 3.0',4,'CAT'],['XDM-FOS100','광 전송',4,'FIBER'],['XDM-SOS100','12G-SDI',4,'SDI'],['XDM-WOS100','HDMI · Wall Overlay',4,'HDMI']]},
      SPX:{name:'Signal Processing eXpert',copy:'여러 공간의 디스플레이를 유연하게.',tags:['HDMI · CATx','TX · RX 전송'],models:['SPX-M810','SPX-M1620','SPX-M3236','SPX-M2472','SPX-M24120'],modelNotes:['8 × 10 · 2U','16 × 20 · 4U','32 × 36 · 7U','24 × 72 · 8U','24 × 120 · 8U'],input:[['SPX-HIS8','HDMI',8,'HDMI']],output:[['SPX-HOS10','HDMI',10,'HDMI'],['SPX-HOS12','HDMI',12,'HDMI'],['SPX-COS12','CATx',12,'CAT']]},
      VDM:{name:'Variety Digital Matrix',copy:'다양한 인터페이스를 연결하는 선택.',tags:['HDMI · SDI','CAT · 광 전송'],models:['VDM-8X','VDM-16X','VDM-32X','VDM-48X','VDM-64X','VDM-80X','VDM-128X','VDM-180X','VDM-288X'],modelNotes:['8 입력 / 8 출력','16 입력 / 16 출력','32 입력 / 32 출력','48 입력 / 48 출력','64 입력 / 64 출력','80 입력 / 80 출력','128 입력 / 128 출력','180 입력 / 180 출력','288 입력 / 288 출력'],input:[['HIS4-U','HDMI',4,'HDMI'],['CIS4-U','HDBaseT',4,'CAT'],['FIS4-U','광 전송',4,'FIBER'],['SIS4-U','3G/HD-SDI',4,'SDI']],output:[['HOS4-U','HDMI',4,'HDMI'],['HOS4S-UW','HDMI · Scaling / Wall',4,'HDMI'],['QOS4S-U','HDMI · Quad View',2,'HDMI'],['COS4-U','HDBaseT',4,'CAT'],['FOS4-U','광 전송',4,'FIBER'],['SOS4','3G/HD-SDI',4,'SDI']]}
    };
    const labels=['제품군','섀시','입출력 카드','전송기','구성 검토','내보내기'];
    const slots=[{id:'in-a',label:'입력 A',dir:'input'},{id:'in-b',label:'입력 B',dir:'input'},{id:'out-a',label:'출력 A',dir:'output'},{id:'out-b',label:'출력 B',dir:'output'}];
    let state={step:0,maxStep:0,family:'XDM',model:null,placements:{},links:{},slot:'in-a',format:'PDF'};
    const design={tone:'warm',density:'comfortable'};
    const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
    function card(id){return [...families[state.family].input,...families[state.family].output].find(c=>c[0]===id)}
    function slotCard(id){return card(state.placements[id])}
    function totals(){let input=0,output=0;for(const s of slots){const c=slotCard(s.id);if(c){if(s.dir==='input')input+=c[2];else output+=c[2]}}return {input,output,ext:Object.values(state.links).reduce((sum,l)=>sum+(l.device?l.count:0),0)}}
    function choices(s,c){if(state.family==='XDM'){if(c[0]==='XDM-CIS100')return ['XDM-CTR100 · TX','XDM-CT103'];if(c[0]==='XDM-COS100')return ['XDM-CTR100 · RX','XDM-CR103'];if(c[0]==='XDM-FIS100')return ['XDM-FT101'];if(c[0]==='XDM-FOS100')return ['XDM-FR101']}if(c[0]==='SPX-COS12')return ['SPX-RX'];return []}
    function persist(){if(window.openai?.setWidgetState)window.openai.setWidgetState({modelContent:{design:'RTCOM Matrix configurator draft',family:state.family,model:state.model,step:labels[state.step],placements:state.placements},privateContent:{rtcomDraft:state,design}}).catch(()=>{})}
    function restore(saved){const s=saved?.privateContent?.rtcomDraft;if(!s||!families[s.family])return;state.family=s.family;state.step=Math.max(0,Math.min(5,Number(s.step)||0));state.maxStep=Math.max(state.step,Math.min(5,Number(s.maxStep)||0));state.model=families[s.family].models.includes(s.model)?s.model:null;state.placements={};state.links={};for(const sl of slots){const c=families[s.family][sl.dir].find(x=>x[0]===s.placements?.[sl.id]);if(c)state.placements[sl.id]=c[0];const l=s.links?.[sl.id];if(c&&l&&choices(sl,c).includes(l.device)){state.links[sl.id]={device:l.device,count:Math.max(0,Math.min(c[2],Number(l.count)||0)),distance:['10','30','50','100','300','2000'].includes(l.distance)?l.distance:'30'}}}state.slot=slots.some(x=>x.id===s.slot)?s.slot:'in-a';state.format=['PDF','CSV','JSON'].includes(s.format)?s.format:'PDF';if(!state.model&&state.step>1)state.step=1;const d=saved?.privateContent?.design;if(d){design.tone=d.tone==='blue'?'blue':'warm';design.density=d.density==='compact'?'compact':'comfortable'}}
    function heading(k,title,desc){return `<div class="rt-eyebrow">${k}</div><h2>${title}</h2><p class="rt-description">${desc}</p>`}
    function familyView(){return heading('01 / PRODUCT FAMILY','연결의 시작, 제품군을 선택하세요.','사용 환경에 맞는 제품군을 고르고, 섀시와 전송기를 함께 구성합니다.')+`<div class="rt-family-grid">${Object.entries(families).map(([id,f])=>`<button type="button" class="rt-family" data-family="${id}" aria-pressed="${state.family===id}"><span class="rt-family-visual"><img src="${assets[id]}" alt="${id} 제품군"></span><span class="rt-family-body"><span class="rt-family-title">${id}<span class="rt-radio" aria-hidden="true">${state.family===id?'✓':''}</span></span><span class="rt-family-en">${f.name}</span><span class="rt-family-copy">${f.copy}</span><span class="rt-tags">${f.tags.map(t=>`<span class="rt-tag">${t}</span>`).join('')}</span></span></button>`).join('')}</div><div class="rt-bottom-note"><span>제품군별로 카드와 전송기 선택 항목이 달라집니다.</span><span>01 — 06</span></div>`}
    function chassisView(){const f=families[state.family];return heading('02 / CHASSIS','구성의 중심이 될 섀시를 선택하세요.',`${state.family} 제품군 · 메인프레임 선택`)+`<div class="rt-split"><div class="rt-model-list">${f.models.map((m,i)=>`<button class="rt-model" data-model="${m}" type="button" aria-pressed="${state.model===m}"><span><strong>${m}</strong><small>${f.modelNotes[i]}</small></span><span class="rt-radio" aria-hidden="true">${state.model===m?'✓':''}</span></button>`).join('')}</div><div class="rt-preview"><div class="rt-preview-label"><span class="rt-eyebrow">${state.family} SERIES</span><span class="rt-pill">제품군 미리보기</span></div><h3>${state.model||'섀시를 선택해 주세요'}</h3><div class="rt-photo"><img src="${assets[state.family]}" alt="${state.family} 제품군 전체 라인업 사진"></div><p class="rt-caption">${f.name}</p><div class="rt-line"></div><p class="rt-caption">사진은 제품군 라인업입니다.<br>다음 화면의 슬롯은 배치 방식을 보여주는 예시입니다.</p></div></div>`}
    function cardsView(){const selectedSlot=slots.find(s=>s.id===state.slot);const t=totals();return heading('03 / INPUT & OUTPUT','슬롯을 눌러 입출력 카드를 구성하세요.',`${state.model} · 카드 선택 방식 미리보기`)+`<div class="rt-editor"><div class="rt-board"><div class="rt-board-top"><span>후면 슬롯 편집</span><span class="rt-pill">개념 도식</span></div><div class="rt-slots">${slots.map(s=>{const c=slotCard(s.id);return `<button type="button" class="rt-slot ${c?'rt-filled':''}" data-slot="${s.id}" aria-pressed="${state.slot===s.id}"><span class="rt-slot-no">${s.label}</span><span><span class="rt-slot-name">${c?c[0]:'카드 추가'}</span><span class="rt-slot-detail">${c?`${c[1]} · ${c[2]} 포트`:s.dir==='input'?'INPUT CARD':'OUTPUT CARD'}</span></span><span class="rt-slot-plus" aria-hidden="true">${c?'↗':'+'}</span></button>`}).join('')}</div><div class="rt-counts"><span><b>${t.input}</b>입력 포트</span><span><b>${t.output}</b>출력 포트</span></div><p class="rt-board-note">입력·출력 각 2칸은 조작 예시입니다. 실제 섀시의 슬롯 수·위치와 카드 설치 가능 여부는 확정 전입니다.</p></div><aside class="rt-picker" aria-label="카드 선택"><div class="rt-picker-head"><span>${selectedSlot.label} · 카드 선택</span></div><div class="rt-picker-list">${families[state.family][selectedSlot.dir].map(c=>`<button type="button" class="rt-choice" data-card="${c[0]}" aria-pressed="${state.placements[state.slot]===c[0]}"><strong>${c[0]}</strong><small>${c[1]} · ${c[2]} 포트</small></button>`).join('')}</div><button type="button" class="rt-button rt-quiet" data-action="remove" ${slotCard(state.slot)?'':'disabled'}>이 슬롯 비우기</button></aside></div>`}
    function linksView(){const remote=slots.filter(s=>['CAT','FIBER'].includes(slotCard(s.id)?.[3]));return heading('04 / EXTENDERS','전송기를 연결하고, 거리를 지정하세요.','입력측은 송신기 TX, 출력측은 수신기 RX를 연결합니다.')+`<div class="rt-link-list">${remote.length?remote.map(s=>{const c=slotCard(s.id),opts=choices(s,c),l=state.links[s.id]||{device:'',count:0,distance:'30'};return `<section class="rt-link"><div class="rt-link-heading"><span>${s.label} · ${c[0]}</span><span class="rt-caption">${c[2]}포트 중 ${l.device?l.count:0}개 연결</span></div><div class="rt-chain">${s.dir==='input'?`소스 <span>→</span><strong>송신기 TX</strong><span>→</span>${c[0]}`:`${c[0]}<span>→</span><strong>수신기 RX</strong><span>→</span>디스플레이`}</div>${opts.length?`<div class="rt-fields"><label>전송기<select data-link="device" data-owner="${s.id}"><option value="">연결하지 않음</option>${opts.map(o=>`<option ${l.device===o?'selected':''}>${o}</option>`).join('')}</select></label><label>연결 수량<select data-link="count" data-owner="${s.id}" ${l.device?'':'disabled'}>${Array.from({length:c[2]+1},(_,i)=>`<option value="${i}" ${l.count===i?'selected':''}>${i}대</option>`).join('')}</select></label><label>케이블 거리<select data-link="distance" data-owner="${s.id}">${(c[3]==='CAT'?['10','30','50','100']:['30','100','300','2000']).map(d=>`<option value="${d}" ${l.distance===d?'selected':''}>${d} m</option>`).join('')}</select></label></div><p class="rt-link-note">같은 모델을 포트 1번부터 순서대로 배정하는 예시 · 케이블 종류·거리·급전 적합성은 아직 검증하지 않습니다.</p>`:`<div class="rt-notice">${c[3]==='CAT'?(s.dir==='input'?'CT101-U · CT102-U · CT103-U-H · CT104-U':'CR101-U · CR102-U · CR103-U · CR104-U'):(s.dir==='input'?'FT101-U · FT102-U · FT103-U-H':'FR101-U · FR102-U · FR103-U')}<br>해당 카드와의 개별 호환 관계 확인 후 선택을 활성화합니다.</div>`}</section>`}).join(''):`<div class="rt-empty">CAT 또는 광 카드를 선택하면 포트에 연결할 전송기가 표시됩니다.<br><button type="button" class="rt-button" data-jump="2" style="margin-top:14px">입출력 카드 선택으로</button></div>`}</div>${state.family==='SPX'?'<div class="rt-notice">SPX-TX는 전송기 그룹에 포함합니다. 입력 카드 직결 또는 TX/RX를 통한 HDMI 연장 경로는 호환 확인 후 활성화합니다.</div>':''}<p class="rt-board-note">HDMI 포트를 TX/RX 쌍으로 연장하는 선택은 상세 설계에서 추가합니다.</p>`}
    function bom(){const rows=[];if(state.model)rows.push(['메인프레임',state.model,1]);for(const s of slots){const c=slotCard(s.id);if(c){const old=rows.find(r=>r[1]===c[0]);if(old)old[2]++;else rows.push([s.dir==='input'?'입력 카드':'출력 카드',c[0],1])}const l=state.links[s.id];if(l?.device&&l.count){const name=l.device.split(' · ')[0],old=rows.find(r=>r[1]===name);if(old)old[2]+=l.count;else rows.push(['전송기',name,l.count])}}return rows}
    function table(){return `<div class="rt-table-wrap"><table><thead><tr><th>구분</th><th>모델</th><th>수량</th></tr></thead><tbody>${bom().map(r=>`<tr><td>${r[0]}</td><td>${r[1]}</td><td>${r[2]}</td></tr>`).join('')}</tbody></table></div>`}
    function reviewView(){const t=totals();return heading('05 / REVIEW','선택한 구성을 한눈에 확인하세요.','매트릭스와 전송기를 하나의 구성 목록으로 정리합니다.')+`<div class="rt-review-head"><div class="rt-review-stat">선택한 메인프레임<strong>${state.model}</strong></div><div class="rt-review-stat">카드 포트 합계<strong>${t.input} IN / ${t.output} OUT</strong></div><div class="rt-review-stat">연결한 전송기<strong>${t.ext}대</strong></div></div>${table()}<div class="rt-notice">디자인 초안 · 실제 설치·호환·급전 검증 전<br>부속품과 케이블 구매 목록은 포함하지 않은 구성 예시입니다.</div>`}
    function exportView(){const data={status:'DESIGN_DRAFT',family:state.family,chassis:state.model,placements:state.placements,extenders:state.links};const content=state.format==='PDF'?`<div class="rt-paper-title">Matrix Configuration</div><p class="rt-caption">${state.family} / ${state.model}</p><div class="rt-line"></div>${table()}<p class="rt-board-note">포트별 연결표 · 케이블·전원 조건 · 검증 내역<br>실제 출력에 포함할 항목</p>`:`<pre>${esc(state.format==='JSON'?JSON.stringify(data,null,2):'Category,Model,Quantity\n'+bom().map(r=>r.join(',')).join('\n'))}</pre>`;return heading('06 / EXPORT','구성을 저장하고 공유하세요.','연락처 입력 없이 구성 요약과 장비 목록을 내보내는 흐름입니다.')+`<div class="rt-export"><div class="rt-export-options">${[['PDF','구성 요약 · 슬롯·전송기 연결'],['CSV','장비 목록 · 모델별 수량'],['JSON','구성 저장 · 다시 불러오기']].map(([id,n])=>`<button type="button" class="rt-format" data-format="${id}" aria-pressed="${state.format===id}"><div><strong>${id}</strong><span>${n}</span></div><span class="rt-radio" aria-hidden="true">${state.format===id?'✓':''}</span></button>`).join('')}<p class="rt-board-note">출력 화면 미리보기입니다.<br>실제 파일 생성은 구현 단계에 포함합니다.</p></div><div class="rt-paper"><div class="rt-preview-label"><span>RTCOM</span><span class="rt-pill">초안 미리보기</span></div>${content}</div></div>`}
    function applyDesign(){root.style.setProperty('--rt-accent',design.tone==='blue'?'light-dark(#276d94,#87c3e8)':'light-dark(#bd591d,#f4a46b)');root.style.setProperty('--rt-tint',design.tone==='blue'?'light-dark(#edf5fa,#202e3a)':'light-dark(#fcf3eb,#33291f)');root.querySelector('.rt-main').style.paddingTop=design.density==='compact'?'22px':'';root.querySelector('.rt-main').style.paddingBottom=design.density==='compact'?'20px':''}
    function render(){nav.innerHTML=labels.map((label,i)=>`<button type="button" class="rt-step" data-jump="${i}" ${i===state.step?'aria-current="step"':''} ${i>state.maxStep?'disabled':''}><i aria-hidden="true">${i<state.step?'✓':String(i+1).padStart(2,'0')}</i><span>${label}</span></button>`).join('');main.innerHTML=[familyView,chassisView,cardsView,linksView,reviewView,exportView][state.step]();root.querySelector('.rt-summary').innerHTML=`<strong>${state.family}</strong>${state.model?' / '+state.model:' 제품군'}<br>${state.step>1?'디자인 초안 · 실제 구성 검증 전':'카테고리: 매트릭스'}`;const next=root.querySelector('[data-action=next]');next.disabled=state.step===1&&!state.model;next.querySelector('span').textContent=['섀시 선택','입출력 카드 구성','전송기 연결','구성 검토','출력 미리보기','처음으로'][state.step];root.querySelector('[data-action=back]').hidden=state.step===0;applyDesign()}
    function changed(){render();persist()}
    root.addEventListener('click',event=>{const b=event.target.closest('button');if(!b||!root.contains(b)||b.disabled)return;if(b.dataset.family){if(state.family!==b.dataset.family){state.family=b.dataset.family;state.model=null;state.placements={};state.links={};state.maxStep=0;state.slot='in-a'}changed();return}if(b.dataset.model){if(state.model!==b.dataset.model){state.model=b.dataset.model;state.placements={};state.links={};state.maxStep=1}changed();return}if(b.dataset.slot){state.slot=b.dataset.slot;changed();return}if(b.dataset.card){state.placements[state.slot]=b.dataset.card;delete state.links[state.slot];changed();return}if(b.dataset.format){state.format=b.dataset.format;changed();return}if(b.dataset.jump!==undefined){const n=Number(b.dataset.jump);if(n<=state.maxStep){state.step=n;changed()}return}if(b.dataset.action==='remove'){delete state.placements[state.slot];delete state.links[state.slot];changed();return}if(b.dataset.action==='back'){state.step=Math.max(0,state.step-1);changed();return}if(b.dataset.action==='next'){state.step=state.step===5?0:state.step+1;state.maxStep=Math.max(state.maxStep,state.step);changed()}});
    root.addEventListener('change',event=>{const select=event.target;if(!select.dataset.link)return;const id=select.dataset.owner;const old=state.links[id]||{device:'',count:0,distance:'30'};if(select.dataset.link==='device'){old.device=select.value;old.count=select.value?Math.max(old.count,1):0}else if(select.dataset.link==='count'){old.count=Number(select.value)}else old.distance=select.value;state.links[id]=old;changed()});
    restore(window.openai?.widgetState);render();
    window.addEventListener('openai:set_globals',event=>{if(event.detail?.globals?.widgetState){restore(event.detail.globals.widgetState);render()}});
    if(globalThis.Tweak){const tweak=new Tweak({container:root,onChange:()=>{applyDesign();persist()}});tweak.addSelect(design,'tone',{label:'강조색',options:[{label:'따뜻한 오렌지',value:'warm'},{label:'차분한 블루',value:'blue'}]});tweak.addSelect(design,'density',{label:'화면 여백',options:[{label:'기본',value:'comfortable'},{label:'간결하게',value:'compact'}]})}
  })();
  